# Phase 3: BLE provisioning replaces Kconfig WiFi credentials.
# This Kconfig file is intentionally empty — WiFi creds now come from the
# user's phone via the official ESP BLE Provisioning app.
#
# If you need build-time tunables (e.g. PoP override), add them here.
